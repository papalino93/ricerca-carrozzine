import { PageSkeleton } from "@/components/PageSkeleton";

/** Vedi PageSkeleton. Qui la navigazione è già nel layout dell'area
 *  amministrazione, quindi basta lo scheletro del contenuto. */
export default function Loading() {
  return <PageSkeleton />;
}
